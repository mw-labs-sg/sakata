# Build-time only. Nothing here runs in the browser.
pandas>=2.2
numpy>=1.26
yfinance>=0.2.40
curl_cffi>=0.7
lxml>=5.0
beautifulsoup4>=4.12
streamlit>=1.35
# zoneinfo has no tz database of its own. Windows ships none at all, and a slim
# Linux image can be missing /usr/share/zoneinfo, so sk_calendar's
# ZoneInfo("America/New_York") raises ZoneInfoNotFoundError at import and takes
# the whole app down. Unconditional rather than platform-marked: the package is
# inert when a system database exists.
tzdata>=2024.1
